version: '3.9'
services:
  smarter-web-frontend:
    image: 127.0.0.0:5000/smarter-web-frontend:0.9.0
    ports:
      - "43080:80"
    deploy:
      placement:
        constraints:
            - node.role == worker
            - node.hostname != CB-Mass-Node1
    networks:
      - BIG-dataplatform-network

  smarter-web-backend:
    image: 127.0.0.0:5000/smarter-web-backend:0.9.0
    ports:
      - "8081:8081"
    deploy:
      placement:
        constraints:
          - node.role == worker
          - node.hostname != CB-Mass-Node1
    networks:
      - BIG-dataplatform-network

networks:
  BIG-dataplatform-network:
    external: true
    name: BIG-dataplatform-network
